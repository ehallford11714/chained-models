[project]
name = "chained-models"
version = "0.1.0"
description = "Chained Models: hierarchical PageIndex retrieval with trained hop routers between specialists."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["torch"]

[project.scripts]
chained-models = "pagechain.__main__:main"
pagechain = "pagechain.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["pagechain*"]
